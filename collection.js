// Collection :- storing multiple values in one variable
// -- objects | key values

let obj = {
  //* Make a small container in memory
  //* Key Value pairs | comma separated
  name: "Aditya",
  age: 18,
  developer: true,
};

// -- array | serialized values | Index - value pairs

let arr = ["a", "b", "c"]; // 0 = 'a' , 1 = 'b' , 2 = 'c'

// Method = object ke andar ke functions
// Property = values| string , boolean , number , collections
// datatype | functions
